@page "/dashboard"
@page "/"
@using WMS_.Data.Entities
@* @inject WMS_.Services.InventoryService InventoryService *@

@* <div class="flex flex-col gap-gutter">
    <!-- Page Header -->
    <div class="flex items-end justify-between">
        <div>
            <h2 class="font-headline-lg text-headline-lg text-on-surface">Dashboard Overview</h2>
            <p class="font-body-md text-body-md text-on-surface-variant mt-1">Real-time inventory and warehouse metrics.</p>
        </div>
        <button class="hidden md:flex items-center gap-2 px-4 py-2 bg-primary text-on-primary rounded-md font-label-md text-label-md hover:bg-primary-container transition-colors shadow-sm">
            <span class="material-symbols-outlined" style="font-size: 18px;">download</span>
            Export Report
        </button>
    </div>
    <!-- Top Stats Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-4">
        <!-- Stat Card 1 -->
        <div class="elevation-1 p-[16px] flex flex-col gap-3">
            <div class="flex justify-between items-start">
                <div class="p-2 bg-primary-fixed rounded-md text-primary">
                    <span class="material-symbols-outlined">inventory_2</span>
                </div>
                <span class="font-label-md text-label-md text-primary flex items-center gap-1 bg-primary-fixed/50 px-2 py-0.5 rounded-full">
                    <span class="material-symbols-outlined" style="font-size: 14px;">trending_up</span> +12%
                </span>
            </div>
            <div>
                <div class="font-body-md text-body-md text-on-surface-variant mb-1">Total Products</div>
                <div class="font-display-lg text-display-lg text-on-surface">@totalProducts</div>
            </div>
        </div>
        <!-- Stat Card 2 -->
        <div class="elevation-1 p-[16px] flex flex-col gap-3 border-t-2 border-t-error">
            <div class="flex justify-between items-start">
                <div class="p-2 bg-error-container rounded-md text-error">
                    <span class="material-symbols-outlined" style="font-variation-settings: 'FILL' 1;">warning</span>
                </div>
                <span class="font-label-md text-label-md text-error flex items-center gap-1 bg-error-container/50 px-2 py-0.5 rounded-full">
                    <span class="material-symbols-outlined" style="font-size: 14px;">arrow_downward</span> Alert
                </span>
            </div>
            <div>
                <div class="font-body-md text-body-md text-on-surface-variant mb-1">Low Stock Items</div>
                <div class="font-display-lg text-display-lg text-on-surface">@lowStockCount</div>
            </div>
        </div>
        <!-- Stat Card 3 -->
        <div class="elevation-1 p-[16px] flex flex-col gap-3">
            <div class="flex justify-between items-start">
                <div class="p-2 bg-secondary-fixed rounded-md text-on-secondary-fixed">
                    <span class="material-symbols-outlined">warehouse</span>
                </div>
                <span class="font-label-md text-label-md text-primary flex items-center gap-1 bg-primary-fixed/50 px-2 py-0.5 rounded-full">
                    <span class="material-symbols-outlined" style="font-size: 14px;">trending_up</span> Active
                </span>
            </div>
            <div>
                <div class="font-body-md text-body-md text-on-surface-variant mb-1">Total Stock Items</div>
                <div class="font-display-lg text-display-lg text-on-surface">@totalStockQty</div>
            </div>
        </div>
        <!-- Stat Card 4 -->
        <div class="elevation-1 p-[16px] flex flex-col gap-3">
            <div class="flex justify-between items-start">
                <div class="p-2 bg-tertiary-fixed rounded-md text-on-tertiary-fixed">
                    <span class="material-symbols-outlined">percent</span>
                </div>
                <span class="font-label-md text-label-md text-on-surface-variant flex items-center gap-1 bg-surface-variant/50 px-2 py-0.5 rounded-full">
                    <span class="material-symbols-outlined" style="font-size: 14px;">horizontal_rule</span> Fill Rate
                </span>
            </div>
            <div>
                <div class="font-body-md text-body-md text-on-surface-variant mb-1">Warehouse Fill Rate</div>
                <div class="font-display-lg text-display-lg text-on-surface">@fillRate.ToString("0.0")%</div>
            </div>
        </div>
        <!-- Stat Card 5 -->
        <div class="elevation-1 p-[16px] flex flex-col gap-3">
            <div class="flex justify-between items-start">
                <div class="p-2 bg-surface-container-highest rounded-md text-on-surface">
                    <span class="material-symbols-outlined">payments</span>
                </div>
                <span class="font-label-md text-label-md text-primary flex items-center gap-1 bg-primary-fixed/50 px-2 py-0.5 rounded-full">
                    <span class="material-symbols-outlined" style="font-size: 14px;">trending_up</span> Stable
                </span>
            </div>
            <div>
                <div class="font-body-md text-body-md text-on-surface-variant mb-1">Operational Status</div>
                <div class="font-headline-md text-headline-md text-success mt-1">Optimal</div>
            </div>
        </div>
    </div>
    <!-- Bento Grid: Charts & Alerts -->
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-gutter">
        <!-- Left Col: Charts (8 spans) -->
        <div class="lg:col-span-8 flex flex-col gap-gutter">
            <!-- Main Line Chart -->
            <div class="elevation-1 p-[24px] h-[320px] flex flex-col">
                <div class="flex justify-between items-center mb-6">
                    <h3 class="font-headline-md text-headline-md text-on-surface">Import/Export Activity</h3>
                    <div class="flex gap-2">
                        <span class="flex items-center gap-1 font-label-md text-label-md text-on-surface-variant"><div class="w-2 h-2 rounded-full bg-primary"></div> Import</span>
                        <span class="flex items-center gap-1 font-label-md text-label-md text-on-surface-variant"><div class="w-2 h-2 rounded-full bg-tertiary"></div> Export</span>
                    </div>
                </div>
                <div class="flex-1 relative w-full border-b border-l border-outline-variant/30 flex items-end pt-4 pb-2">
                    <!-- Abstract line chart representation -->
                    <svg class="absolute bottom-0 w-full h-[80%] px-4" preserveaspectratio="none" viewbox="0 0 100 100">
                        <path d="M0,80 Q10,60 20,70 T40,40 T60,50 T80,20 T100,10 L100,100 L0,100 Z" fill="url(#grad1)" opacity="0.2"></path>
                        <path d="M0,80 Q10,60 20,70 T40,40 T60,50 T80,20 T100,10" fill="none" stroke="#6366f1" stroke-width="2"></path>
                        <path d="M0,90 Q15,85 30,60 T50,70 T70,40 T90,60 T100,50" fill="none" stroke="#ec4899" stroke-dasharray="4" stroke-width="2"></path>
                        <defs>
                            <lineargradient id="grad1" x1="0%" x2="0%" y1="0%" y2="100%">
                                <stop offset="0%" style="stop-color:#6366f1;stop-opacity:1"></stop>
                                <stop offset="100%" style="stop-color:#6366f1;stop-opacity:0"></stop>
                            </lineargradient>
                        </defs>
                    </svg>
                    <!-- Grid lines -->
                    <div class="absolute inset-0 flex flex-col justify-between pointer-events-none opacity-20">
                        <div class="w-full h-px bg-outline"></div>
                        <div class="w-full h-px bg-outline"></div>
                        <div class="w-full h-px bg-outline"></div>
                        <div class="w-full h-px bg-outline"></div>
                    </div>
                </div>
                <div class="flex justify-between font-label-md text-label-md text-outline mt-2 px-4">
                    <span>Mon</span><span>Tue</span><span>Wed</span><span>Thu</span><span>Fri</span><span>Sat</span><span>Sun</span>
                </div>
            </div>
        </div>
        <!-- Right Col: Alerts (4 spans) -->
        <div class="lg:col-span-4 flex flex-col">
            <div class="elevation-1 h-full flex flex-col overflow-hidden">
                <div class="p-4 border-b border-outline-variant bg-surface-container-lowest flex justify-between items-center">
                    <h3 class="font-headline-md text-headline-md text-on-surface flex items-center gap-2">
                        <span class="material-symbols-outlined text-error">campaign</span> Critical Alerts
                    </h3>
                    <span class="bg-error text-on-error font-label-md px-2 py-0.5 rounded-full">Active</span>
                </div>
                <div class="flex-1 overflow-y-auto p-2 flex flex-col gap-2">
                    @if (lowStockCount > 0)
                    {
                        <div class="p-3 rounded-md bg-error-container/20 border border-error/20 flex gap-3 items-start cursor-pointer hover:bg-error-container/40 transition-colors">
                            <div class="p-1.5 bg-error text-on-error rounded-full mt-0.5">
                                <span class="material-symbols-outlined" style="font-size: 16px;">error</span>
                            </div>
                            <div>
                                <h4 class="font-label-md text-label-md text-on-surface">Low Stock Warning</h4>
                                <p class="font-body-md text-body-md text-on-surface-variant text-sm mt-1">There are @lowStockCount items running low on stock. Please check product catalog.</p>
                                <span class="font-data-mono text-data-mono text-xs text-outline mt-2 block">Just now</span>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="p-3 rounded-md bg-surface border border-outline-variant flex gap-3 items-start">
                            <div class="p-1.5 bg-secondary-fixed text-on-secondary-fixed rounded-full mt-0.5">
                                <span class="material-symbols-outlined" style="font-size: 16px;">check_circle</span>
                            </div>
                            <div>
                                <h4 class="font-label-md text-label-md text-on-surface">All Stock Normal</h4>
                                <p class="font-body-md text-body-md text-on-surface-variant text-sm mt-1">No items currently below critical threshold.</p>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
    <!-- Recent Activity Table -->
    <div class="elevation-1 flex flex-col overflow-hidden">
        <div class="p-4 border-b border-outline-variant flex justify-between items-center bg-surface-container-lowest">
            <h3 class="font-headline-md text-headline-md text-on-surface">Recent System Log Activities</h3>
        </div>
        <div class="overflow-x-auto w-full">
            <table class="w-full text-left border-collapse">
                <thead>
                    <tr class="bg-surface border-b border-outline-variant text-on-surface-variant font-label-md text-label-md">
                        <th class="py-3 px-4 font-semibold w-1/3">Activity / Description</th>
                        <th class="py-3 px-4 font-semibold">Action</th>
                        <th class="py-3 px-4 font-semibold">Responsible</th>
                        <th class="py-3 px-4 font-semibold text-right">Date/Time</th>
                    </tr>
                </thead>
                <tbody class="font-body-md text-body-md text-on-surface">
                    @if (recentLogs != null && recentLogs.Any())
                    {
                        @foreach (var log in recentLogs)
                        {
                            <tr class="border-b border-outline-variant/50 hover:bg-surface-container-lowest/50 transition-colors group cursor-pointer">
                                <td class="py-3 px-4 flex items-center gap-3">
                                    <div class="w-8 h-8 rounded bg-surface-container flex items-center justify-center text-outline group-hover:text-primary transition-colors">
                                        <span class="material-symbols-outlined" style="font-size: 18px;">history</span>
                                    </div>
                                    <span class="font-medium">@log.NewValue</span>
                                </td>
                                <td class="py-3 px-4">
                                    <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-md text-xs font-semibold bg-tertiary-fixed text-on-tertiary-fixed-variant border border-tertiary/20">
                                        @log.Action
                                    </span>
                                </td>
                                <td class="py-3 px-4 flex items-center gap-2">
                                    <div class="w-6 h-6 rounded-full bg-secondary text-on-secondary flex items-center justify-center text-xs font-bold">
                                        @(log.UserId.Length > 0 ? log.UserId.Substring(0, Math.Min(2, log.UserId.Length)).ToUpper() : "SY")
                                    </div>
                                    @log.UserId
                                </td>
                                <td class="py-3 px-4 text-right font-data-mono text-data-mono text-on-surface-variant">@log.Timestamp.ToString("yyyy-MM-dd HH:mm")</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="4" class="py-4 text-center text-on-surface-variant">No recent activity logged in the database. Perform some transactions to generate logs.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@code {
    private int totalProducts = 0;
    private int lowStockCount = 0;
    private int totalStockQty = 0;
    private double fillRate = 0.0;
    private List<AuditLog> recentLogs = new();

    protected override async Task OnInitializedAsync()
    {
        totalProducts = await InventoryService.GetTotalProductsCountAsync();
        lowStockCount = await InventoryService.GetLowStockAlertCountAsync();
        totalStockQty = await InventoryService.GetTotalStockQtyAsync();
        fillRate = await InventoryService.GetWarehouseFillRateAsync();
        recentLogs = await InventoryService.GetRecentAuditLogsAsync(5);
    }
} *@
