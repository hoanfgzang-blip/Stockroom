<nav class="bg-surface dark:bg-inverse-surface border-r border-outline-variant dark:border-outline h-full w-sidebar-width flex flex-col z-40 hidden md:flex transition-all duration-300 ease-in-out">
    <div class="h-16 flex items-center px-6 border-b border-outline-variant border-opacity-50">
        <div class="flex items-center gap-3">
            <!-- Brand Logo SVG - 32x32 với màu đồng nhất theo design system -->
            <div class="w-8 h-8 flex items-center justify-center text-primary shrink-0">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="currentColor" width="32" height="32" role="img" aria-label="WMS Logo">
                    <polygon points="50,8 90,38 90,92 10,92 10,38"/>
                    <rect x="18" y="60" width="18" height="32" fill="white"/>
                    <rect x="64" y="60" width="18" height="32" fill="white"/>
                    <rect x="24" y="68" width="4" height="24" fill="currentColor"/>
                    <rect x="72" y="68" width="4" height="24" fill="currentColor"/>
                    <rect x="18" y="60" width="18" height="7" fill="currentColor"/>
                    <rect x="64" y="60" width="18" height="7" fill="currentColor"/>
                    <polygon points="38,38 50,52 62,38 58,38 50,47 42,38" fill="white"/>
                    <polygon points="18,38 28,38 18,52" fill="white"/>
                    <polygon points="72,38 82,38 82,52" fill="white"/>
                </svg>
            </div>
            <div>
                <h1 class="font-headline-md text-headline-md font-extrabold text-primary">Warehouse Manager</h1>
            </div>
        </div>
    </div>
    <div class="flex-1 overflow-y-auto py-4 px-3 flex flex-col gap-1">
        <NavLink class="nav-item-link flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all cursor-pointer" href="dashboard" Match="NavLinkMatch.All">
            <span class="nav-icon material-symbols-outlined">dashboard</span>
            <span class="font-label-md text-label-md">Dashboard</span>
        </NavLink>
        <NavLink class="nav-item-link flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all cursor-pointer" href="product-management">
            <span class="nav-icon material-symbols-outlined">qr_code_scanner</span>
            <span class="font-label-md text-label-md">Quét mã &amp; Phân loại</span>
        </NavLink>
        <NavLink class="nav-item-link flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all cursor-pointer" href="warehouse-location-map">
            <span class="nav-icon material-symbols-outlined">location_on</span>
            <span class="font-label-md text-label-md">Locations</span>
        </NavLink>
        <NavLink class="nav-item-link flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all cursor-pointer" href="warehouse-import-export">
            <span class="nav-icon material-symbols-outlined">input</span>
            <span class="font-label-md text-label-md">Quản lý Nhập</span>
        </NavLink>
        <NavLink class="nav-item-link flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all cursor-pointer" href="warehouse-export">
            <span class="nav-icon material-symbols-outlined">output</span>
            <span class="font-label-md text-label-md">Quản lý Xuất</span>
        </NavLink>
        <NavLink class="nav-item-link flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all cursor-pointer" href="inventory-reports">
            <span class="nav-icon material-symbols-outlined">assessment</span>
            <span class="font-label-md text-label-md">Reports</span>
        </NavLink>
        <NavLink class="nav-item-link flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all cursor-pointer" href="#">
            <span class="nav-icon material-symbols-outlined">group</span>
            <span class="font-label-md text-label-md">Employees</span>
        </NavLink>
        <NavLink class="nav-item-link flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all cursor-pointer mt-auto" href="system-settings">
            <span class="nav-icon material-symbols-outlined">settings</span>
            <span class="font-label-md text-label-md">Settings</span>
        </NavLink>
    </div>
</nav>

<style>
    /* === Icon System: 3 States === */

    /* Default state */
    .nav-item-link {
        color: #6B7280; /* Gray 500 */
        text-decoration: none;
    }

    .nav-item-link .nav-icon {
        color: #6B7280; /* Gray 500 */
        font-size: 22px;
        transition: color 0.2s ease, font-variation-settings 0.2s ease;
        font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
    }

    /* Hover / Active state */
    .nav-item-link:hover,
    .nav-item-link.active {
        color: #3B82F6; /* Blue 500 */
        background-color: #EFF6FF; /* Blue 50 tint */
    }

    .nav-item-link:hover .nav-icon,
    .nav-item-link.active .nav-icon {
        color: #3B82F6; /* Blue 500 */
        font-variation-settings: 'FILL' 1, 'wght' 500, 'GRAD' 0, 'opsz' 24;
    }

    /* Active state: left accent border */
    .nav-item-link.active {
        border-left: 3px solid #3B82F6;
        font-weight: 700;
    }

    /* Disabled state (used via .disabled class if needed) */
    .nav-item-link.disabled,
    .nav-item-link[disabled] {
        color: #D1D5DB; /* Gray 300 */
        pointer-events: none;
        cursor: not-allowed;
    }

    .nav-item-link.disabled .nav-icon,
    .nav-item-link[disabled] .nav-icon {
        color: #D1D5DB; /* Gray 300 */
    }
</style>
